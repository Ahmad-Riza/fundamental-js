// array -> index array selalu mulai dari 0
const names = ["Reza", "Naruto", "Doraemon", "Ejen Ali", "Patkai"];

// names.splice(1, 0);
// names.length;
// names.reverse();
// names[2] = undefined;

console.log(names);

// array di dalam array
const grid = [
  [1, 2, 3], // index ke 0
  [4, 5, 6], // index ke 1
  [7, 8, 9] // index ke 2
];

console.log(grid[2][1]);
